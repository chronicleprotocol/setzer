#!/usr/bin/env bash
set -e
pair=wbtcgsu
sources=(
gsu
)

case $1 in
   gsu) {
    setzer x-price "$1" "WBTC"
      };;

  *) {
    export SETZER_MIN_MEDIAN=1
    setzer --price-commands "-$1-" $pair "${sources[@]}"
  };;
esac
